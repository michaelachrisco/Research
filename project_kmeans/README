#Michael Chrisco
#information about the k-means script
#DEPENDENCIES:
#This system was made and tested with Debian. 
#1)gnuplot (you can do a sudo-apt-get install gnuplot)
#2)update your system before running the scripts.

#To run the script, go into the directory for the script and type either sh Main.bash
#or make Main.bash executable by chmod 757 Main.bash and  ./Main.bash

#Main.bash can delete all documents for new information by running it with the -D option.
#sh Main.bash -D 
#Make sure you want to delete all files before you do this operation. 
